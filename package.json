{
  "name": "snapkey",
  "version": "0.1.0",
  "private": true,
  "description": "Dateien und Nachrichten verschlüsselt direkt zwischen zwei Geräten übertragen, ohne Server und ohne Konto",
  "license": "MIT",
  "bin": {
    "snapkey": "bin/snapkey.js"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.js\""
  }
}
